# MCHA 设计决策记录

> 本文档独立维护项目的全部设计决议与文档修订历史，面向开发过程与答辩备查。
> 面向用户的正式设计见 `docs/project-design.md`，其中不出现决议编号与版本注释。

## 编号规则

- **D1–D10、D15**：自旧版设计沿用，继续有效（个别条目有修订，见表内标注）。
- **D100 起**：v2 重构（Agent-First）新增决议。
- **D11–D14、D16–D29**：属归档实现主线（`archive/v0.12-mvp-line`），不再使用，避免编号歧义。

## 沿用决议

| # | 决议 | 内容 | v2 状态 |
| --- | --- | --- | --- |
| D1 | 界面形态 | CLI/TUI；Web（只读状态页）列 P2 | 沿用，深化为多轮会话（D101） |
| D2 | Java 供给 | 全自动受管安装，不降级；设计见 project-design §8.7 | 沿用，执行主体变为 `ensure_java` 工具 |
| D3 | 价格表 | 内置常见模型预设随包分发，注明来源与日期，用户可覆盖 | 沿用 |
| D4 | 数据目录 | `~/.mcha/`（Windows：`%APPDATA%\mcha\`；原名 `~/.mc-host-agent/`，D15 定名时统一更名，不迁移旧目录） | 沿用 |
| D5 | 基岩跨平台 | 不做 | 沿用 |
| D6 | 提案提交方式 | ~~tool-calling（`submit_spec` 工具）交卷~~ | **废除**：ServerSpec 不再是流程关卡（D105）；结构化提交精神由工具参数 Schema 普遍承载 |
| D7 | Forge | MVP 不做；P2 或降级为安装指导 | **修订**（2026-09-02，随 v2.0）：分步纳入——M2.1 实现 Vanilla / Paper / Spigot / Fabric 四渠道，Forge 走指导模式（Skill 写明人工步骤概要 + 建议确认 Fabric 替代），正式 Forge 渠道 M2.x 补 |
| D8 | LLM SDK | 不引入，自研薄客户端 | 沿用 |
| D9 | 内网穿透选型 | 樱花frp 默认；自建 frp / Tailscale 为 P2 备选；playit 不做 | 沿用，编排主体变为 Agent + `tunnel_*` 工具 |
| D10 | 定制内容体系 | 五层载体（代码/数据/API/指南/Prompt + 错误模式库）；不引入 RAG / embedding | **修订**：L0"决策树进 Rust 代码"废除，Skills 升为一级公民（D104）；其余沿 project-design §8.5 |
| D15 | 命名规范 | 正式名 **Minecraft Host Agent**，仓库 `minecraft-host-agent`，简称 **MCHA**（行文）/**`mcha`**（标识符）；Cargo 包名 `minecraft-host-agent`，二进制/CLI 命令 `mcha`；数据目录 `~/.mcha/`；环境变量 `MCHA_API_KEY` / `MCHA_DATA` / `MCHA_WORKSPACE`；技术概念 "Agent" 不属于产品命名不改 | 沿用 |

## v2 新决议

| # | 决议 | 内容 |
| --- | --- | --- |
| D100 | **Agent-First 总纲** | Agent Loop 是唯一执行引擎、全阶段运行；一切副作用由 LLM 经工具调用发起、Rust 工具后端执行；Rust 职责 = Loop + 工具 + 校验 + 安全边界，不做任务编排。旧 NFR-1（"LLM 只做需求理解、副作用不出 Rust"）废除。废因：归档线产品被定性为"LLM 问询员 + 自动化脚本"，非 Agent |
| D101 | 多轮会话式交互 | CLI REPL 为主形态，会话持续、无任务轮次边界；`mcha new "…"` 为预填首条消息的快捷方式；废除"≤3 轮澄清"上限；失控防护改由轮数保险丝（默认 40）+ 预算守卫承担 |
| D102 | 先框架、后场景 | M1 实现通用 Agent 框架（Loop + 通用工具 + REPL + R5/R6 骨架），M2 叠加开服场景包（领域工具 + 知识库 + Skills + 提示词）；禁止在框架就绪前先写场景流程代码 |
| D103 | web_search 后端 | 工具接口 P0 定义；默认无后端（如实告知用户），可选 DuckDuckGo HTML 抓取（免 key，脆弱性自担）或 Serper 等（配 key）；领域事实主通道为知识库 + 上游 API |
| D104 | Skills 定制内容 | 领域流程知识写成 `assets/skills/<name>/SKILL.md`（决策指南、诊断规程、操作手册），`load_skill` 按需加载；替代旧"决策树 Rust 引擎"；保留红线"版本事实不进 Prompt" |
| D105 | ServerSpec 降格 | ServerSpec 保留为部署档案（Profile）格式，由 Agent 经 `save_profile` 工具落盘、`load_profile` 读回复用；不再是决策树输出物或流程关卡 |
| D106 | 确认门与路径收敛 | 工具按 Permission 分级（ReadOnly/Write/Execute/Network）；写与执行默认确认、可按会话授权；文件路径收敛工作区与数据目录；`confirm_level` 可配（paranoid/standard/auto） |
| D107 | CLI 会话界面 | inline 滚动流（不采用全屏 TUI）+ 语义化渲染块（思考 / 工具调用 / 结果 / 确认 / 用量分块，规范见 project-design §8.6）；风格对标 Claude Code / Codex；渲染器为事件总线订阅者，与 store 落盘同源；M1 交付 |
| D108 | 用量展示时机 | 会话过程中不刷用量行；会话结束（`/exit` / Ctrl-D / 提示符处 Ctrl-C）时打印一次汇总（输入 / 输出 token、费用、时长）+ 轨迹文件路径。R6"清晰展示"由三层满足：退出汇总、`mcha usage` 全局账本、`sessions show` 每次调用明细；预算告警 / 超限中断即时提示例外。计量层（UsageRecord 逐调用强制生成）不受影响 |
| D109 | 回合原子性 | 上下文裁剪以完整回合为单位；打断时未完成的 tool_calls 回填"用户中断"错误后入史，半截 assistant 文本丢弃——会话消息流任何时刻结构合法，可直接导出 / 恢复 |
| D110 | 确认门交互粒度 | 确认弹窗三选一：y 本次允许 / a 本会话允许此工具 / n 拒绝（拒绝结构化回传，Agent 自行调整）；auto 模式全免（演示 / CI） |
| D111 | 部署前确定性校验 | `check_plan` 工具：Agent 部署前调用，按 checklist 确定性核对方案完整性（online-mode 与账号一致、Java 匹配、内存、白名单配套等）；"不漏分支"由决策指南约束 + 确定性验收双保险 |
| D112 | LLM 客户端细则 | 429/5xx 指数退避重试 ≤2 次且计入调用次数；连接 15s / 单次整体 300s；thinking 计量照收入输出 token、思考全文不入史（渲染后仅留占位）；价格表缺失模型费用记 0 并标注 |
| D113 | 配置与首次启动 | 首次运行检测无配置自动进入 `setup` 向导：必填仅 3 项（endpoint 预设快捷项 / 模型名 / API Key 隐藏输入写 .env），其余默认值；完成后自动连接测试（最小对话请求验证连通与延迟）再进入会话；`config set`（toml_edit 保注释）/ `list` / `test` 子命令；启动校验缺失项打印可复制模板。继承归档主线两段式向导的实测设计（原 D12/D14 思路） |
| D114 | 渲染块折叠与可读性 | 真机试用反馈的 UI 细节定稿：① 思考流直显超 4 行折叠（2000 字符保险丝），"已思考 Ns"在思考结束时立即挂出（不迟到到正文末尾）；② 工具调用行加动词短语（run_command → "运行命令"）；③ 命令输出直显超 12 行省略并注明行数，结果摘要压单行 + 渲染层 120 字符保险丝；④ 回合内跨板块自动补空行、提示符前留空行。**折叠 / 截断只影响终端显示**：完整内容照常落盘（R5）与回传模型 |
| D115 | 下载镜像默认 | Mojang 资源默认走 BMCLAPI（域名重写实现），Adoptium 二进制默认清华 TUNA；`[network]` 配置段可切 `off` / 自定义；实际使用的域记入轨迹；白名单域同步扩充（§8.10/§12）。裁定理由：国内直连不稳定，默认保首次成功率 |
| D116 | sys_info 工具 | 新增只读环境探测工具（OS / 架构 / 总内存 / 可用内存 / CPU 核数，sysinfo crate）：-Xmx 推荐依据，后续诊断复用；免确认、跨平台、可单测 |
| D117 | Java 供给拆分 | `check_java`（ReadOnly 探测：PATH / JAVA_HOME / 受管目录）与 `ensure_java`（Network 安装）拆为两个工具——确认门是工具级静态属性，混装会迫使纯探测也弹确认 |
| D118 | 服务器交付语义 | start_server 托管进程由 mcha 管理（Drop 守卫防孤儿，mcha 退出即停）；服务器长期运行以交付的 start.bat / start.sh 为准——"Agent 演示期间管理，最终交付物是目录 + 脚本"；下载 jar 统一改名 server.jar（原始名入轨迹）。**托管模型已被 D134 独立窗口模型修订**（server.jar 改名语义保留） |
| D119 | 离线白名单 ack | check_plan 中"离线模式配白名单"为默认要求；用户明确拒绝时 Agent 传 `whitelist_disabled_ack=true` 表示已确认风险并放行留痕——确定性闸门与用户意愿两全 |
| D120 | 领域检索通道 | 新增 `wiki_search` / `wiki_page`（ReadOnly）+ `[retrieval]` 来源注册；mcwiki 后端（MediaWiki API，实测可用）随 M2.1 落地；mcmod 后端（HTML 解析，无官方 API）随 M2.2 mod 步骤落地、规格本批写入 §8.11。事实优先级红线扩展：版本存在性 / 下载 URL / 哈希以上游 API 为权威，Wiki / 百科定位为背景知识、交叉验证与中文语境补充 |
| D121 | M2 分步实施 | M2.1 = 服务器设施（四渠道 + 检索通道 + Skill + 场景提示词），出口标准 US1 精简版（版本 + 账号 + 端类型 → 127.0.0.1:25565 可登录）；mod 安装（FR-12/13）、Profile（FR-16 save/load）、穿透（FR-17）、诊断（FR-18）后置 M2.2 / M2.3。裁定理由：小步迭代、决策细致不跑偏 |
| D122 | M2.2 范围与 FR-13 形态 | mod 自动化落地 **Fabric**（`fetch_server_jar` 已支持；Forge 维持 D7 指导模式，NeoForge 不做）；FR-13 自然语言推荐**不加新工具**——Agent 把玩法偏好翻译为多组英文搜索词调 `search_mods`、汇总候选经 `ask_user` 确认后走 resolve→install，规程写入 server-setup Skill（零新增代码，Agent-First 标准形态） |
| D123 | mod 工具三段式 | `search_mods`（ReadOnly 检索）/ `resolve_mod`（ReadOnly 确定性解析：别名→精确匹配→版本匹配→依赖闭包→意图清单）/ `install_mods`（Network 唯一落盘点）职责分离——检索可多轮试探、解析无副作用可重试、确认门只落在真实安装（D117 同理） |
| D124 | 安装期权威重取 + 冲突策略 | manifest 只承载**意图**（slug + version_id），`install_mods` 经 `GET /v2/versions?ids=[..]` 实时重取 URL 与 sha1/sha512 再下载校验——LLM 转手抄错/篡改不影响正确性，"下载 URL / 哈希以上游 API 为权威"红线贯彻到安装期；下载域仅 `cdn.modrinth.com`（API 下发 url 域强校验）；同名文件**同哈希跳过**（安装幂等、中断重跑安全）/ **异哈希报错**（不静默覆盖用户手动安装，交 Agent 问用户） |
| D125 | mod 中文别名表 | L1 TOML（`assets/knowledge/mod_aliases.toml`）：`[[mods]] slug/name/aliases[]`，初版 15–20 个高频 mod（暮色森林、JEI、Create、Sodium 等），带来源与采集日期；双通道（别名命中直查 slug 免检索歧义，未命中走 /v2/search）；零命中如实报错（如 OptiFine 非 Modrinth 项目）；只承载"叫法→项目"映射、不承载版本事实（红线不变） |
| D126 | mcmod 后端范围 | `scraper` 选择器解析搜索页（标题 / 英文别名 / 简介 / class 地址）+ 主页**摘要级**正文（`strip_html` 兜底）；版本适配表**不解析**（版本事实以 Modrinth 为权威，适配表后置扩展）；URL 前缀强约束（仅 `search.mcmod.cn/s` 与 `www.mcmod.cn/class\|item/<id>.html` 形态）；约 800ms 间隔自律、解析失败结构化报错 |
| D127 | Profile 定稿 | 字段定稿（§8.6：含 mods 明细 [{slug, version_id, file_name, sha1}] 与 artifacts [{kind, path}]）；`save_profile`=Write（确认门显示摘要）/ `load_profile`=ReadOnly（profile_id 或 latest 读回上下文）；落盘 `~/.mcha/profiles/<YYYYMMDD-HHMMSS>.json` + `mcha profiles list/show` 薄壳子命令；定位不变：记录产物与复用载体，不是流程关卡（D105） |
| D128 | check_plan mod 项 | checklist 增第 ⑨ 项：software=fabric 且带 mod 意图清单时，逐项重验 version 的 `game_versions` / `loaders` 覆盖目标 mc_version 与 loader——对 resolve 输出的确定性复核 |
| D129 | 依赖闭包算法 | Modrinth `dependencies` 中 `required` 项入闭包（optional/incompatible/embedded 不入）；BFS 展开 + project_id 去重 + 环检测 + 深度上限（默认 10）；版本选取 = `game_versions` 含目标 MC 且 `loaders` 含目标加载器的最新版；每项记录 `dependency_of` 链与入选理由（轨迹可回放，R5） |
| D130 | Modrinth 覆盖边界实测 | 实现期实测（2026-09-03）：暮色森林（Twilight Forest）为 CurseForge 独占发行、Modrinth 无官方收录（同类的还有 OptiFine 自有分发）→ 此类需求走别名表「无收录名单」（`mod_aliases.toml` `[[unlisted]]`）：零命中时给出如实结构化解释与替代建议，不假装能装；演示与验收用例从「暮色森林 + JEI」调整为「JEI + Sodium」。裁定理由：下载事实以上游 API 为权威的红线不允许为覆盖 CurseForge 引入无官方 API 的第三方抓取渠道；CurseForge 官方 API 需申请 key，留作后置扩展 |
| D131 | CurseForge 通道接入 | **官方 API v1**（`api.curseforge.com`，`x-api-key` 头）+ **每用户一次性可选 key**（`MCHA_CURSEFORGE_KEY` 存 `.env`，环境装配时读取一次随 ToolCtx 下发；key 不入 config.toml 与仓库）。用户决策过程留痕：①"所有用户走开发者 key 的中心代理"被裁定排除——违反 CF 条款（key 禁止转售/共享访问）、配额按 key 单桶（全用户共享必 429）、单点运维、key 实质公开；②"绕过 API 直接爬网站"被裁定排除——Cloudflare 反爬墙、页面无权威哈希（拆掉下载安全三重防线）、JS 重度渲染易碎且违反 ToS；官网抓取仅允许用于无任何官方渠道的场景（getbukkit→Spigot 逃生舱先例）。技术方案：Modrinth 优先自动降级（零命中且 key 已配置时转 CF 检索）；别名表 `[[mods]]` 增 `source` 字段（暮色森林→ the-twilight-forest 直达 CF，从 `[[unlisted]]` 移入）；manifest 增 `source` + 源专属 id（Modrinth `version_id` / CF `mod_id+file_id`）；install 按源分发批量重取 + 源专属域白名单（`mediafilez.forgecdn.net` / `media.forgecdn.net`）+ CF sha1 单哈希校验（如实标注哈希强度差异）；`downloadUrl` 为 null（未开放分发）结构化报错指向手动页；限额自律（key 级配额较 Modrinth 紧，批量端点优先）；未配置 key 时如实降级 + 分步申请指引（同樱花frp token 引导模式），Modrinth 通道不受影响。加载器范围不变（仅 Fabric，Forge 指导模式不动） |
| D132 | setup 重运行 / 启动自检 | 用户补充的三项配置体验裁定：① **setup 可重复运行**——读取已有配置作为默认值（回车保留），已有 Key 显示"已设置，回车保留"；② **`.env` 合并写入**——只更新被修改的 Key，其余原样保留（修复旧版整文件覆盖会抹掉其他 Key 的缺陷）；③ **setup 增可选步骤**——CurseForge API Key 配置（默认跳过，选择时给分步申请指引，网址按终端能力渲染为可点击链接：OSC 8 白名单检测 + 纯文本回退 + `MCHA_NO_HYPERLINK=1` 强制关闭；工具回传 Agent 的文本保持纯 URL）；④ **启动自检（非阻断）**——交互会话启动时一次性提示未配置的可选项（CurseForge Key 缺失 / 搜索后端未配置等），不强制、不重复提醒、全部就绪不输出 |
| D133 | CurseForge 国内镜像通道（MCIMirror） | 背景：用户实测无法访问 CurseForge 开发者门户（portal 访问不了，网络不可达），key 获取无门；用户提议参考 PCL2 启动器生态的解法。裁定：**接入开源公益镜像 `https://mod.mcimirror.top/curseforge`（mcmod-info-mirror 项目）作为无 key 默认通道**——与官方 CF API v1 同构（检索/详情/文件列表/批量重取 schema 一致，免 key），数据为官方 API 透传（实测 sha1 与实文件核验一致，27MB 暮色森林 jar 17MB/s 下载成功）。通道选择：已配 `MCHA_CURSEFORGE_KEY` → 官方；未配置 → 自动走镜像（原"未配置 key 如实降级"语义废除，CF 独占 mod 开箱可用）。与 D131 否决"中心代理"的边界：当时否决的是**自建** key 池转发（持他人 key、共享配额桶、条款风险）；此处为使用既有公益镜像，不持 key、不碰配额。技术配套：检索统一改 `GET /v1/mods/search`（官方文档原生支持，镜像仅支持 GET、POST 404——实测原 POST 版从未对官方真实验证过）；下载域白名单增 `edge.forgecdn.net`（实测 API 下发 downloadUrl 即 edge 域，302 重定向到 mediafilez 域，现白名单会拒）与 API 域 `mod.mcimirror.top`（仅元数据不下文件）；轨迹如实标注"经社区镜像"。实测排除的替代方案留痕：mcmod.cn 下载页（`/download/{id}.html`）静态 HTML 无直链、下载需服务端图像验证码（`DownloadCaptcha`）+ token + 临时授权 URL——自动化即绕过反爬控制，技术不稳定且属 D131 网页抓取同一红线，否决；暮色森林 GitHub 官方仓库零 release（不发 jar），否决；BMCLAPI 仓库不可达且无 CF 镜像端点（404 实测），否决。出口标准升级：**暮色森林场景零配置闭环**（未配置任何 CF key 不须人工介入完成解析安装） |
| D134 | start_server 独立窗口模型（修订 D118） | 用户实测裁定：`start_server` 改为**在独立终端窗口运行交付的 start 脚本**（Windows `CREATE_NEW_CONSOLE` 新开 cmd 跑 `start.bat`；Unix 探测 x-terminal-emulator / gnome-terminal / konsole / xfce4-terminal / xterm 跑 `start.sh`，无图形环境结构化报错；测试用启动器闭包注入无头 mock）——与手动双击完全一致，D118"演示期管理 / 长期运行脚本"双轨矛盾就此消解。**服务器日志不进 Agent 界面**（只在窗口滚动）；就绪判定轮询 `logs/latest.log`（mtime 晚于启动前快照防旧日志假就绪——本轮实测复现该陷阱：起服等待被上一轮 latest.log 的 `Done (` 瞬间"假就绪"）+ `Done (` 特征；每 5s 一行进度（R4）；Esc / Ctrl-C 仅打断等待不影响窗口；超时区分"有新日志无就绪特征"（附尾部）与"日志毫无新写入"（启动即失败嫌疑）。**`stop_server` 移除**：停服交用户在服务器窗口 Ctrl+C（或输入 stop），mcha 退出不再连带停服（原 D118 Drop 守卫作废）；就绪交付话术必须说明停服方法（用户要求）。`server_status` 收敛为轻量只读（端口探测 + 日志尾部）。重复开服守卫：server-port 已监听（TCP connect）即拒绝。同轮修复 mc_ping 长期 bug：SLP status 响应解析漏读 **String 的 VarInt 长度前缀**（MC 协议所有 String 字段均带前缀；本地起真服 vanilla 1.21.1 抓包实证载荷 `81 01 7b …` = VarInt(129)+JSON）——此前假服务器与客户端共用自研编码自洽、单测通过，对真实服务器必然报"status 响应不是合法 JSON: expecting value at line 1 column 1"（用户长期观测症状）；修复含 packet_id 校验与假服务器响应形状改规范形 |
| D135 | M2.3 方案定稿：frpc 独立窗口 + API v4 事实基线 | 开工前抓取樱花frp **API v4 OpenAPI 规范**（`api.natfrp.com/docs/openapi.yaml`，v4.1.0，AGPL-3.0 仅 HTTP 调用不复制定义代码）逐端点核实：`/user/info`（实名 / 用户组等级 / 流量 / 隧道额度 / 冻结状态独立 schema）、`/nodes`（flag 位掩码）+ `/node/stats`（在线 / 负载）、`GET+POST /tunnels`、`/tunnel/delete`、`/user/data_plans`、`/system/clients`（frpc 官方下载 URL + **MD5** + size，下载域 `nya.globalslb.net`）；frpc 启动语法官方确认 `frpc -f <访问密钥>:<隧道ID>`（配置自动拉取）。**frpc 进程模型裁定：独立终端窗口**（修订 §8.8 原"进程托管 + Drop 守卫"）——与 D134 服务器模型同构、mcha 退出不断隧道、无孤儿管理；就绪信号改**轮询 API `online=true`**（免解析 frpc 日志）；重复拉起以"隧道已在线"守卫。natfrp 文档"AI 应引导用户用 GUI 启动器"的提示如实留痕取舍：MCHA 是自动化 Agent，GUI 启动器不可编程驱动，frpc CLI + API v4 即官方为程序化管理设计的路径 |
| D136 | 樱花frp token 双入口 | 访问密钥（`MCHA_NATFRP_TOKEN` 存 `.env`，永不入 config.toml 与仓库）：① **setup 向导可选步骤**（同 D132 交互：状态行 → 申请指引可点击链接 → Password 隐藏输入 → `merge_env_file` 合并写入）；② **REPL `/token` 斜杠命令**（会话进行中补配，隐藏输入、密钥不经过 LLM 上下文，写入后同步进程环境与 AgentEnv）。指引含**注册与登录两个入口**（`/auth/register` 与 `/auth/login`，用户补充）+ 实名认证 + 密钥获取页。**启动自检不纳入** natfrp token（仅跨网络场景需要，平时是噪音）；引导发生在 Agent 网络拓扑分支内 |
| D137 | tunnel_* 七件套 | `check_tunnel`（ReadOnly：/user/info + /user/data_plans 账号快照 + frpc 在位检查）/ `ensure_frpc`（Network：/system/clients 按 OS+ARCH 选包 → 下载 MD5 校验 → 落 `~/.mcha/runtime/frpc/<ver>/`，同版本幂等跳过）/ `select_tunnel_node`（ReadOnly：/nodes + /node/stats 硬过滤 + 打分 → top N 附理由）/ `create_tunnel`（Network 确认门：名称约定 **`mcha-mc<本地端口>`**，GET /tunnels 查重复用，POST /tunnels 创建）/ `start_tunnel`（Execute 确认门：独立窗口拉 frpc → 轮询 online → TCP connect + mc_ping 端到端验证）/ `tunnel_status`（ReadOnly：诊断快照）/ `delete_tunnel`（Network 强确认门：换节点重建与演示清理）。确认门与轨迹导出对 `-f <token>:<id>` 形态打码；`download_verified` 补 Md5 哈希变体（md-5 crate 已有） |
| D138 | 节点打分与选择交互 | 节点选择为**工具内确定性算法**（硬过滤：在线、可建隧道 bit2、非强制访问认证 bit8、非私有 bit6、用户组等级 ≥ 节点 VIP；排序：内地优先 bit3 → 负载低 → 非 BETA bit10 → uptime 稳定）→ Agent 经 `ask_user` 呈现 top N（默认推荐第一位）由用户确认——公网暴露类操作的用户知情原则。`remote` 端口留空由平台自动分配（实现期验证项），`/nodes` 顶层键形态同列验证项 |
| D140 | 启动自检纳入樱花frp 提示 | **修订 D136**"启动自检不纳入 natfrp token"的决定：`print_startup_hints` 增加第三条——未配置 `MCHA_NATFRP_TOKEN` 时提示"本机联机不受影响；需要朋友跨网络加入时运行 `/token` 或 `mcha setup`（含注册/登录/实名分步指引）"。非阻断灰字、每会话一次，与 CurseForge / 搜索后端两条提示同构；实测体验证明开机提示成本可忽略、且比"进入穿透分支才发现没配密钥"少一轮往返 |
| D143 | 樱花frp 指引链接勘误 | 用户实测 `/auth/register` 与 `/auth/login` 深链失效（站点为 SPA，入口在首页右上角）：setup 向导、`/token`、`check_tunnel` 三处指引统一改为单一首页入口 `https://www.natfrp.com/`（注册 / 登录），实名认证（`/user/`）与密钥页（`/user/profile`）链接保留。修订 D136 中"注册与登录两个入口"的 URL 形态（双入口交互不变，均落在首页） |
| D142 | 环境引导脚本恢复 | 自归档分支 `archive/v0.12-mvp-line`（原 D13/FR-18）恢复 `scripts/bootstrap.sh`（Linux/macOS）与 `scripts/bootstrap-windows.ps1`（Windows）：幂等搭好 rustup 工具链（Windows 经 winget 装 Rustup + MSVC Build Tools C++ 工作负载）并 `cargo install --path .`，末尾提示 `mcha setup` / `mcha new`。v2 适配仅头部注释（决议引用改注归档来源）；README「构建与运行」章节置顶引用。属分发基础设施，不触及 Agent 行为 |
| D141 | 服务器任务先感知现状（先看后动） | 行事规则增量：涉及服务器 / 部署 / 穿透的任务，第一步必须感知工作区现状（`list_dir` 工作区：server/、worlds、mods、logs/latest.log；必要时 `mc_ping` 本地端口确认在跑；`list_dir` 数据目录 `~/.mcha/profiles/` 查已有部署档案），**发现已有部署时先向用户陈述现状并确认意图（沿用现有服 / 继续未完成部署 / 确认后新开），不得默认新开**。落点为场景提示词"行事规则"与 server-setup Skill §1 开工前置——纯给 Agent 的知识增量，零新工具零流程代码（符合 D100：这是"给 Agent 什么知识"，不是"写什么流程"） |
| D139 | 穿透安全与白名单增量 | 白名单域 +`api.natfrp.com`（API）与 `nya.globalslb.net`（frpc 下载 CDN）；token 打码覆盖确认门展示、`/token` 回显与 `sessions export`（extra_secrets 纳入 natfrp token）；建删隧道幂等自律（同名称复用不重复创建）；R6 边界澄清：樱花frp 非 AI API 不计 token / 费用，隧道流量为 natfrp 自有计量、经 API 取数展示（连接卡片与 tunnel_status） |

## 设计文档修订历史

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-08-28 | v0.1a | 需求文档初版（原 `project-requirements.md`） |
| 2026-08-28 | v0.1b | 设计文档初版（原设计草稿） |
| 2026-08-28 | v0.2 | 合并为单一活文档；决议 D1–D8；新增 Java 自动供给完整设计 |
| 2026-08-28 | v0.3 | 决议 D9：樱花frp 为穿透默认方案 |
| 2026-08-28 | v0.4 | 新增定制内容分层体系（决议 D10） |
| 2026-09-01 | v0.4.1 | 全局定名规范化（沿用归档主线决议 D15） |
| 2026-09-01 | v1.0 | **v2 重构（Agent-First）**：废除旧 NFR-1 与决策树 Rust 引擎（D6 废除、D10 修订）；确立 Agent Loop 唯一执行引擎、工具调用唯一副作用通道（D100）；多轮会话式交互（D101）；先框架后场景路线（D102）；新增决议 D103–D106；架构、数据流、模块全面重写；Java 供给与穿透编排细节保留为工具后端设计。同日：决议记录与修订历史移出设计文档，独立维护于本文档 |
| 2026-09-01 | v1.1 | 设计文档面向最终用户精简：移除与早期方案的对比性叙述；取消 P0–P2 优先级分层；FR-12 拆分为 mod 清单安装（FR-12）与 mod 自然语言推荐（FR-13），后续编号顺延（原 FR-13–18 → FR-14–19）；故障诊断（定制 4）标记为选做 |
| 2026-09-01 | v1.2 | 新增会话界面设计（D107）：inline 滚动流 + 语义化渲染块规范（§4.2 效果示意、§8.6 详设），FR-06 扩充；选型补 crossterm / termimad |
| 2026-09-01 | v1.3 | 用量展示调整（D108）：会话中不再逐回合显示用量行，改为退出时一次性汇总；明确退出方式（`/exit` / Ctrl-D / Ctrl-C）；FR-08 与 §4.2/§8.6 同步 |
| 2026-09-01 | v1.4 | 设计细化评审：回合原子性与打断语义（D109）；确认门 y/a/n 粒度（D110）；新增 `check_plan` 部署前校验工具（D111）；LLM 客户端重试 / 思考模式 / 价格缺失细则（D112）；run_command / edit_file / web_search / ask_user 语义细化；Profile 字段草案恢复；白名单离线 UUID 实测教训留痕；终端符号降级、斜杠命令与会话恢复交互；Skills 指南以中文撰写 |
| 2026-09-01 | v1.5 | 配置与首次启动设计补全（D113）：setup 向导（必填 3 项 + 连接测试）、配置文件全景（[model]/[[prices]]/[budget]/[safety]/[search]）、config set/list/test、启动校验；FR-05 与 §9 R3 同步 |
| 2026-09-01 | v1.6 | 系统提示词分层澄清：框架级 system prompt（通用角色 + 工具纪律 + 安全规则）属 M1 交付，场景提示词（开服管家角色 + Skills 清单）属 M2；最终 system prompt = 框架基础段（固定）+ 场景段（注入），§8.5 L4 与 §14 同步 |
| 2026-09-01 | v1.7 | **M1 实现期技术选型落定**（§10 表同步）：新增 async-trait（Tool / Interaction / LlmClient 的 dyn 对象安全需要）与 futures-util（Stream 驱动）；取消令牌改为手写 AtomicBool + Notify（约 40 行，答辩可解释）；termimad 移出 M1 选型（Markdown 静态块渲染属 M2 方案摘要场景，届时引入）。M1 Agent 框架实现完成：Loop + 通用工具集 + REPL + R5/R6 骨架，49 项单元 / Loop 级测试，出口标准链路经本地 mock OpenAI SSE 服务端到端验证 |
| 2026-09-01 | v1.8 | 渲染可读性定稿（D114）：思考折叠 / 动词解释 / 输出省略 / 板块空行，§8.6 表同步；实测另修复两处环境缺陷——Windows 下 `mask.rs` 类型推断歧义（E0283）、setup 连接测试嵌套 tokio 运行时 panic |
| 2026-09-01 | v1.9 | 二次 UI 细节：横幅改为分组排版（标题 / 空行 / 三行信息 / 操作提示，去掉工具枚举）；"无价格预设"提示从会话中移至退出汇总一次性给出；确认门改单键（crossterm raw 模式免回车，Esc/Ctrl-C 取消，Windows 只认 Press 事件，非 tty 自动退化行读取）；确认门前空行经新增 `Event::Blank` 由渲染器顺序保证（确认块由交互线程直接打印，跨线程时序脆弱）；"已思考 Ns"补齐斜体样式并加固事件时机（任何非思考增量都立即收起思考段）；实测发现清华平台在正文分块上附带 `reasoning_content: ""`，空字符串需按字段缺失处理（否则思考段一直不闭合、"已思考"迟到到正文末尾），已加 SSE 回归测试 |
| 2026-09-02 | v2.0 | **M2.1 服务器设施方案定稿并落入实现细则**：新增决议 D115–D121（镜像默认 BMCLAPI/TUNA、sys_info、check_java/ensure_java 拆分、服务器交付语义、离线白名单 ack、领域检索通道、M2 分步）；D7 修订为 Forge 分步纳入（M2.1 指导模式）；设计文档新增 §8.10（服务器设施实现细节：版本比较器 / 渠道与镜像表 / 文件生成 / 进程生命周期 / 连通验证 / check_plan checklist）与 §8.11（检索通道：wiki_search / wiki_page，mcwiki 随 M2.1、mcmod 规格留档 M2.2）；FR-20 新增（领域检索通道）；§8.2/8.4/8.5/8.6/8.7/10/11/12/13/14 同步；选型新增 sysinfo / md-5 / scraper（M2.2） |
| 2026-09-02 | v2.1 | **M2.1 服务器设施实现完成（S1–S9）**：knowledge 模块（MC 版本比较器、java_compat / server_software TOML、Mojang / Paper / Fabric / Adoptium 上游客户端）+ 14 个领域工具（check_version_compat / sys_info / check_java / ensure_java / fetch_server_jar / write_server_files / start_server / stop_server / server_status / probe_port / mc_ping / check_plan / wiki_search / wiki_page）；实现期实测修正并同步文档：① Paper 旧 v2 API 已 410 下线，迁移 Fill v3（§8.10/§8.4/§11）；② 26.x 年份版本线为 Java 25（官方 javaVersion 实测），java_compat 收拢 1.20.5 开区间并新增 26.x 区间；③ B站 Wiki 无 TextExtracts，正文走 parse+去标签；④ 清华 TUNA 拒绝空 UA（403），MCHA 统一 UA=mcha/<版本>；⑤ start_server spawn 对 ETXTBSY 重试（NFR-3）；新增依赖 zip/tar/flate2/sha1/md-5/sysinfo。验证：97 项单元 / Loop 级测试 + 10 项真实上游冒烟全过（JRE 受管安装、四渠道下载含哈希校验、wiki 检索、SLP ping、错误版本就近建议、离线集成流程） |
| 2026-09-03 | v2.2 | **确认门可用性修复（用户实测反馈）**：① 弹窗内容空白——通用摘要生成器只认识 run_command / write_file / http_download 的字段名，领域工具参数名不同取不到值；修复：`Tool` trait 新增 `confirm_summary` 覆写点（§8.2 确认门内容的工具自述），write_server_files / ensure_java / fetch_server_jar / start_server / stop_server 五个工具展示方案要点（目录 / 渠道 / online-mode / 端口 / 白名单 / 命令行），框架层双兜底（覆写为空 → 通用规则 → 参数摘要，绝不弹空框）；② 无操作引导——单键确认路径弹框后直接等待按键，用户不知道按什么；修复：弹框后打印"请按一个键：[y] 本次允许 · [a] 本会话允许此工具 · [n] 拒绝 · [Esc/Ctrl-C] 取消"。回归测试 5 项（各工具确认内容非空且含关键信息） |
| 2026-09-03 | v2.3 | **终端交互四项修复（用户实测反馈）**：① ask_user 提示语反复换行、键入不可见——dialoguer 绘制与渲染线程 / spinner steady-tick 并发输出互相打乱；修复：交互激活闸（`ui_active`，交互期间渲染器停靠排队）+ ask_user 不叠加 spinner（交互控件独占终端），§8.6 同步；② 交付语后日志直接刷出——start_server 读者任务就绪后停止向事件流发送并**放弃发送端**（缓冲保留供 server_status），§8.6 同步；③ 回合结束后无法回到提示符、Ctrl-D 无反应、Ctrl-C 直接杀进程无用量汇总——根因是日志读者长期持有事件发送端，`renderer.await` 永不返回，REPL 卡死在回合收尾（此时 Ctrl-D 无渲染、二次 Ctrl-C 走默认行为杀进程）；①②的放弃发送端修复即解除卡死，提示符恢复后提示符处 Ctrl-D（EOF）/ Ctrl-C（D108 汇总退出）按既有设计生效；④ 上下文窗口默认 128k → **256k**（config 默认值 / 模板 / 文档样例 / README 同步）。新增回归测试：读者放弃发送端、配置默认值 |
| 2026-09-03 | v2.4 | **确认框排版两处修复（用户实测反馈）**：① 确认框贴在未收尾的流式文本同一行（"……修改白名单文件：┌─ 工具 edit_file 要写入文件"）——框由交互线程直接打印，gate 发出的块前空行事件尚在渲染队列未被处理，跨线程抢打印；修复：新增 `Event::QueueDrained` **队列排空握手**——gate 发送哨兵事件并等待渲染器 ack（FIFO 保证此前内容已全部画出；上限 500ms，单元测试无渲染器时限时放行不悬挂），Blank 事件同时收掉未换行的文本行记账；② "替换为"多行内容破坏框线（续行无 `│ ` 前缀）——交互框打印前按内嵌换行展开框行（`expand_box_lines`），每个物理行都有框线。回归测试：框行展开、队列排空经渲染任务置位 ack。**另**：上下文 256k 在存量用户配置上不生效——旧版向导模板把 128000 显式写入了用户的 config.toml（显式配置优先于代码默认值，横幅如实显示旧值）；修复：加载时检测到恰好等于旧默认 128000 则一次性就地迁移为 256000（toml_edit 保留注释重写 + 打印一次性提示；用户显式改过的其他值不动），KNOWN_SECTIONS 补齐 network / retrieval |
| 2026-09-03 | v2.5 | **M2.2 mod 场景包方案定稿**：新增决议 D122–D129（范围裁定 Fabric 落地 / FR-13 不加工具、mod 工具三段式、安装期权威重取与冲突策略、中文别名表、mcmod 后端范围收窄至摘要级、Profile 定稿、check_plan mod 项、依赖闭包算法）；设计文档新增 §8.12（mod 场景实现细节：Modrinth 客户端 / 三段式工具 / 安装语义 / 闭包算法 / 别名表 / FR-13 规程 / Profile 存取）；§8.6 Profile 字段定稿；§8.10 checklist 增 ⑨；§8.11 mcmod 规格按 D126 收窄；§12 白名单追加 api.modrinth.com / cdn.modrinth.com / search.mcmod.cn / www.mcmod.cn；§13 测试清单同步；§14 M2.2 细化为 S1–S7 与出口标准（US1 完整版 mod 增量）。裁定依据：用户确认五项开放点（方案 B / 摘要级 / 跳过-报错 / 不加工具 / 15–20 条） |
| 2026-09-03 | v2.6 | **M2.2 mod 场景包实现完成**：Modrinth v2 上游客户端（search / project / versions / 批量重取，本地 mock 可注入）；`search_mods` / `resolve_mod` / `install_mods` 三段式落地（别名表 17 项 + 无收录名单、BFS 依赖闭包 + 环检测 + 深度上限、安装期实时重取权威 URL/哈希 + sha1/sha512 双校验 + 原子落盘 + 同哈希跳过/异哈希报错）；mcmod 检索后端（scraper 选择器解析搜索页 + 主页摘要，URL 前缀强约束，800ms 间隔自律）；Profile 存取（save_profile / load_profile 工具 + `store/profiles` + `mcha profiles list/show`）；check_plan 增 mod 兼容复核项；server-setup 技能 mod 分支（含推荐规程）、场景提示词与工具动词表更新、FakeLlm 端到端集成测试。实现期实测暮色森林 CurseForge 独占（D130），验收用例与文档示例同步调整。验证：131 项单元 / Loop 级测试 + 14 项真实上游冒烟全过（含 Modrinth 真实解析安装、MC百科检索、兼容性复核） |
| 2026-09-03 | v2.7 | **CurseForge 通道方案定稿（D131）与配置体验补充（D132）**：经用户三轮决策（接入意愿 → 中心代理方案排除 → 网页抓取方案排除 → 官方 API + 每用户一次性可选 key 落定）。设计文档 §8.12 新增 CurseForge 通道与双源解析流程（Modrinth 优先自动降级、别名表 `source` 标注、manifest 扩展、按源安装与哈希策略、未配置 key 的如实降级）；§8.2 三段式表、§8.6 key 存储、§11 数据源表、§12 白名单（forgecdn 两域）、§13 测试清单、§14 实现顺序同步；§4.2 示例对话更新为双源解析形态。D132：setup 可重复运行（预填已有配置）+ `.env` 合并写入 + setup 可选 CF key 步骤 + 启动自检（非阻断）。实现待用户确认后进行 |
| 2026-09-03 | v2.8 | **CurseForge 通道与配置体验实现完成**：CurseForge 官方 API v1 客户端（search / mod files / files 批量重取；camelCase 对齐、403/429 结构化报错、sha1 提取与 required 依赖归一）；ToolCtx / AgentEnv 贯通 `curseforge_key`（.env 装配读取）+ `[network] curseforge_api` 测试钩子；`mods.rs` 双源化——别名表 `source` 字段（暮色森林迁移至 CurseForge 正册）、resolve 零命中自动降级 CF、跨源依赖闭包（DepRef 归一）、manifest 增 source + 源专属 id、install 按源分发（CF forgecdn 域白名单 + sha1 校验 + downloadUrl null 如实报错）；check_plan ⑨ 扩展到 CF 清单；setup 向导重做（预填已有配置、`.env` 合并写入、可选 CF key 步骤 + 申请指引）；REPL 启动自检（非阻断提示可选项）；场景提示词与 SKILL 同步（CF 引导话术）。验证：clippy 0 错、138 项单元 / Loop 测试 + 15 项真实冒烟全过（CF 真实冒烟需配置 MCHA_CURSEFORGE_KEY，无 key 优雅跳过） |
| 2026-09-03 | v2.9 | **CurseForge 国内镜像通道定稿（D133）**：用户反馈 portal 不可达、key 获取无门，提议参考 PCL2 生态解法。实测 PCL-CE 源码定位到 MCIMirror（mcmod-info-mirror）并逐端点验证：与官方 CF API v1 同构、免 key、文件列表过滤参数生效、批量重取可用、forgecdn 下载 17MB/s 且 sha1 核验一致；唯一不兼容为 POST search（镜像 404，官方原生支持 GET——检索统一改 GET 官方/镜像通吃）。裁定接入 `mod.mcimirror.top/curseforge` 为无 key 默认通道（官方 key 通道保留优先）；同轮实测否决 mcmod.cn 下载页（图像验证码 + token 临时授权，绕过即对抗反爬）、暮色森林 GitHub（零 release）、BMCLAPI（无 CF 镜像端点）。设计文档 §8.12（CF 通道改双基址、解析流程去 key 条件、resolve_mod 表、下载白名单加 edge.forgecdn.net）、§11 数据源表、§12 白名单（+edge.forgecdn.net / mod.mcimirror.top）、§3 setup 与自检话术、§13 测试清单、§14 出口标准升级为零配置闭环、§4.2 示例对话同步。实现待用户确认后进行 |
| 2026-09-03 | v2.10 | **start_server 独立窗口模型 + mc_ping 协议修复定稿（D134）**：用户裁定①`start_server` 弹独立终端窗口跑交付 start 脚本（与手动双击一致），服务器日志只在窗口滚动、Agent 界面不再展示；②就绪改轮询 `logs/latest.log`（mtime 防旧日志假就绪 + `Done (` 特征），等待期 5s 一行进度（R4），Esc/Ctrl-C 只打断等待；③`stop_server` 移除，停服由用户在服务器窗口 Ctrl+C（交付话术须说明停服方法），mcha 退出不再杀服务器（D118 Drop 守卫作废）；④`server_status` 收敛为端口探测 + 日志尾部轻量只读；⑤重复开服以端口监听守卫。同轮修复 mc_ping 长期报"非法 JSON"：SLP status 帧解析漏读 String 的 VarInt 长度前缀——本地真服（vanilla 1.21.1）抓包实证 `81 01 7b …` 后修复，假服务器测试响应形状同步改规范形。设计文档 §4.2 示例对话、FR-14 映射、工具表、§3 能力边界、§8.10 进程生命周期块重写与 mc_ping 行、§12 进程安全例外、§13 测试清单同步 |
| 2026-09-04 | v3.1 | **M2.3 实现收尾两处增量**：D140 启动自检纳入樱花frp 密钥提示（修订 D136 的排除决定）；D141 服务器任务"先看后动"行事规则（场景提示词 + Skill 文案，发现已有部署不得默认新开）。同日随 D135–D139 完成实现与 Windows 弹窗句柄继承勘误（`CREATE_NEW_CONSOLE` + 默认句柄继承改走 `start` 内建 + stdio 置空），全量测试 162 + 真实冒烟 18 通过；同日 D142 自归档分支恢复环境引导脚本（scripts/bootstrap*.sh/ps1，rustup + MSVC 一键装 + cargo install）、D143 勘误樱花frp 注册 / 登录指引链接（深链失效，统一指向首页）
| 2026-09-04 | v3.0 | **M2.3 内网穿透方案定稿**：开工前抓取樱花frp API v4 OpenAPI 规范逐端点实测（D135 事实基线），经用户五项决策（frpc 独立窗口 / token 双入口 / 七件套细粒度 / ask_user 节点确认 / delete_tunnel 纳入）落定 D135–D139。设计文档 §8.8 重写为定稿（含 setup 引导 + 会话编排 + 排障三阶段用户流程）；§8.2 工具表、§8.6 配置（MCHA_NATFRP_TOKEN + `/token` 命令 + 向导可选步骤）、§11 数据源表、§12 白名单与打码、§13 测试清单、§14 实现顺序（S1–S6）同步。实现待本批文档确认后进行 |
